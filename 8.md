单元测试8
题量: 10 满分: 100.0 截止时间: 2026-06-21 23:55:00 （已截止）

最终成绩100分
第1次作答 本次成绩100分
一. 单选题（共4题）
1
【单选题】
如果程序需要连续访问大型数组中的每个元素，不适宜采用的存储器寻址方式是:
A、
寄存器间接寻址
B、
寄存器相对寻址
C、
直接寻址
D、
寄存器变址寻址
我的答案：C
10.0分
AI讲解
2
【单选题】下面的汇编指令是C语言函数void convert(int (*a)[        ],int* b, int m, int n)对应的汇编指令，C语言函数空白处是(        )
# r8d=m，r9d=n ，rcx = a， rdx =b
convert:
imul r8d, r9d
mov rax, rcx
jmp .L2
.L3: mov r10d, DWORD PTR [rax]
mov DWORD PTR [rdx], r10d
add rax, 4
lea rdx, 4[rdx]
.L2: movsx r10, r8d
lea r10, [rcx+r10*4]
cmp rax, r10
jb .L3
ret
void convert(int (*a)[        ],int* b,int m, int n){
int *pa=*a, *pb=b, l;
l=m*n;
for(;pa<*a+l; pa++)
(        );
}
A、
pb++=pa;
B、
*pb=*pa
C、
pb=*pa++;
D、
*pb++=*pa
我的答案：D
10.0分
AI讲解
3
【单选题】下面说法错误的是(        )
A、
若ECX=0，则LOOP指令和JECX指令都发生转移。
B、
LOOP指令相当于DEC ECX和JNZ两条指令。
C、
若ECX=0，则Loop指令循环体执行0次。
D、
LOOP指令不影响标志，而DEC指令却会影响除CF之外的其他状态标志。
我的答案：C
10.0分
AI讲解
4
【单选题】

对于字符串string，如果使用寄存器间接寻址“[rbx]”访问其头一个元素，RBX应该赋值为:

A、
字符串长度

B、
字符串末地址

C、
字符串首地址

D、
字符串首地址减1

我的答案：C
10.0分
AI讲解
二. 多选题（共2题）
5
【多选题】
指令“loop cont”可以用两条指令代替，它们是__________。
A、
jz cont
B、
inc ecx
C、
jnz cont
D、
dec ecx
E、
jmp cont
我的答案：CD
10.0分
AI讲解
6
【多选题】
循环结构的程序通常包括______部分。
A、
调用循环
B、
设置循环初值
C、
循环控制
D、
循环返回
E、
循环体
我的答案：BCE
10.0分
AI讲解
三. 判断题（共4题）
7
【判断题】
介绍LOOP指令时，常说它相当于DEC ECX和JNZ两条指令。但考虑对状态标志的影响，它们有差别。LOOP指令不影响标志，而DEC指令却会影响除CF之外的其他状态标志。
我的答案：对
10.0分
AI讲解
8
【判断题】
判断循环是否结束的循环控制部分只能安排在循环体之后。
我的答案：错
10.0分
AI讲解
9
【判断题】

x86处理器的LOOP指令常用于增量计数的循环控制。

我的答案：错
10.0分
AI讲解
10
【判断题】
若ECX=0，则LOOP指令和JECXZ指令都发生转移。
我的答案：对
10.0分
